# Gold Data Layer

Gold is reserved for reviewed, release-ready data artifacts. The current version has no Gold
dataset. Current RAG outputs are Silver because they are processed/generated but not manually
reviewed as final recommendation data.
